# Ruby Gems for building Citrus
# Run `grunt update-gemfile-lock` to update to the latest compatible versions

source 'https://rubygems.org'

group :development, :test do
  gem 'sass', '~> 3.4.19'
  gem 'scss_lint', '~> 0.43'
end